@model ZrakForum.DataAccess.Entities.Thread

@{
    ViewBag.Title = "Show";
}

@if (Model != null)
{
    if (Model.Posts == null)
    {
        <h2>Neuspelo učitavanje teme. Molimo pokušajte kasnije</h2>
    }
    else
    {
        <h2 class="text-dark">@Model.Name</h2>
        <table class="table table-striped table-hover">
            @foreach (var item in Model.Posts)
            {
                <tr>
                    <td class="border-right" style="width: 20%">
                        <span>
                            <img class="img-fluid rounded-circle img-profile"
                                 src="~/wwwroot/img/profile.png" />
                        </span>
                        <br />
                        <span>@item.Author.Username</span>
                        <br />
                        <span>@item.CreatedAt.ToString("dd. MM. yyyy. HH:mm")h</span>
                    </td>
                    <td style="width: 80%">@item.Content</td>
                </tr>
            }
        </table>

        <button class="btn btn-dark mb-2 float-right" id="showPostForm">Dodaj komentar</button>

        <form id="postForm">
            <input type="text" />
        </form>
    }
}
else
{
    <h2>Nema teme sa tim nazivom</h2>
}

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")

    <script>
        $(document).ready(function () {
            $("#postForm").hide();

            $("#showPostForm").click(function () {
                $("#postForm").toggle('slow');
            });
        });
    </script>
}